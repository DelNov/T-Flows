$MeshFormat // same as MSH version 2
  version(ASCII REAL*8; currently 4.1)
    file-type(ASCII INT*4; 0 for ASCII mode, 1 for binary mode)
    data-size(ASCII INT*4; sizeof(INT*8))
  < INT*4 with value one; only in binary mode, to detect endianness >
$EndMeshFormat

$PhysicalNames // same as MSH version 2
  numPhysicalNames(ASCII INT*4)
  dimension(ASCII INT*4) physicalTag(ASCII INT*4) "name"(127 characters max)
  ...
$EndPhysicalNames

$Entities
  numPoints(INT*8) numCurves(INT*8)
    numSurfaces(INT*8) numVolumes(INT*8)
  pointTag(INT*4) X(REAL*8) Y(REAL*8) Z(REAL*8)
    numPhysicalTags(INT*8) physicalTag(INT*4) ...
  ...
  curveTag(INT*4) minX(REAL*8) minY(REAL*8) minZ(REAL*8)
    maxX(REAL*8) maxY(REAL*8) maxZ(REAL*8)
    numPhysicalTags(INT*8) physicalTag(INT*4) ...
    numBoundingPoints(INT*8) pointTag(INT*4) ...
  ...
  surfaceTag(INT*4) minX(REAL*8) minY(REAL*8) minZ(REAL*8)
    maxX(REAL*8) maxY(REAL*8) maxZ(REAL*8)
    numPhysicalTags(INT*8) physicalTag(INT*4) ...
    numBoundingCurves(INT*8) curveTag(INT*4) ...
  ...
  volumeTag(INT*4) minX(REAL*8) minY(REAL*8) minZ(REAL*8)
    maxX(REAL*8) maxY(REAL*8) maxZ(REAL*8)
    numPhysicalTags(INT*8) physicalTag(INT*4) ...
    numBoundngSurfaces(INT*8) surfaceTag(INT*4) ...
  ...
$EndEntities

$PartitionedEntities
  numPartitions(INT*8)
  numGhostEntities(INT*8)
    ghostEntityTag(INT*4) partition(INT*4)
    ...
  numPoints(INT*8) numCurves(INT*8)
    numSurfaces(INT*8) numVolumes(INT*8)
  pointTag(INT*4) parentDim(INT*4) parentTag(INT*4)
    numPartitions(INT*8) partitionTag(INT*4) ...
    X(REAL*8) Y(REAL*8) Z(REAL*8)
    numPhysicalTags(INT*8) physicalTag(INT*4) ...
  ...
  curveTag(INT*4) parentDim(INT*4) parentTag(INT*4)
    numPartitions(INT*8) partitionTag(INT*4) ...
    minX(REAL*8) minY(REAL*8) minZ(REAL*8)
    maxX(REAL*8) maxY(REAL*8) maxZ(REAL*8)
    numPhysicalTags(INT*8) physicalTag(INT*4) ...
    numBoundingPoints(INT*8) pointTag(INT*4) ...
  ...
  surfaceTag(INT*4) parentDim(INT*4) parentTag(INT*4)
    numPartitions(INT*8) partitionTag(INT*4) ...
    minX(REAL*8) minY(REAL*8) minZ(REAL*8)
    maxX(REAL*8) maxY(REAL*8) maxZ(REAL*8)
    numPhysicalTags(INT*8) physicalTag(INT*4) ...
    numBoundingCurves(INT*8) curveTag(INT*4) ...
  ...
  volumeTag(INT*4) parentDim(INT*4) parentTag(INT*4)
    numPartitions(INT*8) partitionTag(INT*4) ...
    minX(REAL*8) minY(REAL*8) minZ(REAL*8)
    maxX(REAL*8) maxY(REAL*8) maxZ(REAL*8)
    numPhysicalTags(INT*8) physicalTag(INT*4) ...
    numBoundingSurfaces(INT*8) surfaceTag(INT*4) ...
  ...
$EndPartitionedEntities

$Nodes
  numEntityBlocks(INT*8) numNodes(INT*8)
    minNodeTag(INT*8) maxNodeTag(INT*8)
  entityDim(INT*4) entityTag(INT*4) parametric(INT*4; 0 or 1)
    numNodesInBlock(INT*8)
    nodeTag(INT*8)
    ...
    x(REAL*8) y(REAL*8) z(REAL*8)
       < u(REAL*8; if parametric and entityDim >= 1) >
       < v(REAL*8; if parametric and entityDim >= 2) >
       < w(REAL*8; if parametric and entityDim == 3) >
    ...
  ...
$EndNodes

$Elements
  numEntityBlocks(INT*8) numElements(INT*8)
    minElementTag(INT*8) maxElementTag(INT*8)
  entityDim(INT*4) entityTag(INT*4) elementType(INT*4; see below)
    numElementsInBlock(INT*8)
    elementTag(INT*8) nodeTag(INT*8) ...
    ...
  ...
$EndElements

$Periodic
  numPeriodicLinks(INT*8)
  entityDim(INT*4) entityTag(INT*4) entityTagMaster(INT*4)
  numAffine(INT*8) value(REAL*8) ...
  numCorrespondingNodes(INT*8)
    nodeTag(INT*8) nodeTagMaster(INT*8)
    ...
  ...
$EndPeriodic

$GhostElements
  numGhostElements(INT*8)
  elementTag(INT*8) partitionTag(INT*4)
    numGhostPartitions(INT*8) ghostPartitionTag(INT*4) ...
  ...
$EndGhostElements

$Parametrizations
  numCurveParam(INT*8) numSurfaceParam(INT*8)
  curveTag(INT*4) numNodes(INT*8)
    nodeX(REAL*8) nodeY(REAL*8) nodeZ(REAL*8) nodeU(REAL*8)
    ...
  ...
  surfaceTag(INT*4) numNodes(INT*8) numTriangles(INT*8)
    nodeX(REAL*8) nodeY(REAL*8) nodeZ(REAL*8)
      nodeU(REAL*8) nodeV(REAL*8)
      curvMaxX(REAL*8) curvMaxY(REAL*8) curvMaxZ(REAL*8)
      curvMinX(REAL*8) curvMinY(REAL*8) curvMinZ(REAL*8)
    ...
    nodeIndex1(INT*4) nodeIndex2(INT*4) nodeIndex3(INT*4)
    ...
  ...
$EndParametrizations

$NodeData
  numStringTags(ASCII INT*4)
  stringTag(string) ...
  numRealTags(ASCII INT*4)
  realTag(ASCII REAL*8) ...
  numIntegerTags(ASCII INT*4)
  integerTag(ASCII INT*4) ...
  nodeTag(INT*8) value(REAL*8) ...
  ...
$EndNodeData

$ElementData
  numStringTags(ASCII INT*4)
  stringTag(string) ...
  numRealTags(ASCII INT*4)
  realTag(ASCII REAL*8) ...
  numIntegerTags(ASCII INT*4)
  integerTag(ASCII INT*4) ...
  elementTag(INT*8) value(REAL*8) ...
  ...
$EndElementData

$ElementNodeData
  numStringTags(ASCII INT*4)
  stringTag(string) ...
  numRealTags(ASCII INT*4)
  realTag(ASCII REAL*8) ...
  numIntegerTags(ASCII INT*4)
  integerTag(ASCII INT*4) ...
  elementTag(INT*8) numNodesPerElement(INT*4) value(REAL*8) ...
  ...
$EndElementNodeData

$InterpolationScheme
  name(string)
  numElementTopologies(ASCII INT*4)
  elementTopology
  numInterpolationMatrices(ASCII INT*4)
  numRows(ASCII INT*4) numColumns(ASCII INT*4) value(ASCII REAL*8) ...
$EndInterpolationScheme

