!==============================================================================!
!   Definition of the macro which avoids too many warnings while compiling.    !
!------------------------------------------------------------------------------!

# if T_FLOWS_DEBUG == 1
#   define Unused(p)  associate(p => p); end associate
# else
#   define Unused(p)
# endif
